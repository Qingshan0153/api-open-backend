
#微服务配置
spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 192.168.200.130:8848
      discovery:
        namespace: groupapi-dev
        group: groupapi
      config:
        group: groupapi
        namespace: groupapi-dev
        file-extension: yaml
        refresh-enabled: false

  profiles:
    # 环境名
    active: dev